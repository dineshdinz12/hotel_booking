import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['example.com', 'images.unsplash.com', '3.imimg.com', 'www.checkatrade.com', 'www.wyndhamhotels.com', 'encrypted-tbn0.gstatic.com' ,'cf.bstatic.com'],
  },
};

export default nextConfig;
